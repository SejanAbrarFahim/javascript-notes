// Ternary operator 
  let age = 18;
  let message = age > 18 ? "You can drive" : "You can't drive" ;
  console.log(message); // you can't drive 

  let ageOne = 17 ;
  let Message = ageOne >= 17 ? "you can drive" : "you can't drive" ;
  console.log(Message); // you can drive 



  // use the javascript ternary operato to perform mulitple statements
    let speed = 90 ;
    let Message1 = speed >=120 ? "too Fast" : speed <= 80 ? "too slow" : "Average You can Drive" ;
    console.log(Message1); // Average You can Drive
    